
# ── AOS-GOV-CACHE (Model B / ADR054) — git-ignored governance cache; refreshed by sync, never committed ──
# Hub is the audit SSoT; version held is recorded in tracked _aos/AOS_GOVERNANCE_VERSION.yaml.
/_aos/governance/
/_aos/methodology/
/_aos/lean-kit/
# ── /AOS-GOV-CACHE ──

# ── Raw materials (git-ignored; can balloon; Drive-synced) — RAW_MATERIALS canon ──
/raw-materials/

# ── Client hub build output (regenerated by scripts/build_sadot_client_hub.py) ──
/hub/dist/

# ── Python build artifacts (harvested scripts under design/, blender/) ──
__pycache__/
*.pyc
